/**
 * Theme constants for non-CSS contexts (canvas/SVG props, QR codes, PWA
 * icons). Mirrors the matching `--paper` / `--ink` custom properties in
 * styles.css — change both together when retheming.
 */
export const PAPER = '#f3e9d0';
export const INK = '#241d12';
export const GOLD = '#c9a227';
export const BLOOD = '#e5484d';
export const NIGHT = '#0b0b10';
